# MonoOto 開発指針

## コミュニケーションと作業範囲

- 日本語で応答する。冒頭は理由や経緯を省いた端的な結論とし、根拠・補足を後に続ける。
- 最初に `SPEC.md` と対象コードを読み、現在の依頼範囲、実装済みの段階、未検証事項を確認する。Gitがある場合はHEADと未追跡ファイルを含む作業ツリーも確認する。
- 必要最小限のファイルだけ変更する。範囲が広がる場合は、変更理由と対象ファイルを先に報告する。利用者の既存変更を保護する。
- 改善提案は一度に一つとする。比較を求められた場合は採用基準と利点・欠点を説明し、推奨する次の一手を一つに絞る。
- 調査・仕様作成の依頼を、実装、インストール、システム設定変更、コミット、公開の許可と解釈しない。許可済みの通常作業については確認を繰り返さない。
- 独立して検証できる課題には必要に応じてサブエージェントを使う。編集の所有範囲を明示し、最終判断は主担当が根拠を確認して行う。

## プロダクトの約束

- 目的は、片耳でステレオ音源を楽しむための音の手がかりを探ること。両耳聴、自然な左右定位、聴力の回復を保証しない。
- 「左右チャンネル由来の違いの判別」「主観的な広がり」「聞きやすさ・音楽の楽しさ」を区別する。音色弁別の成功をステレオ知覚の実証と扱わない。
- `SPEC.md` の根拠、設計上の仮説、実測結果を混同しない。自由音場での単耳聴研究を、そのまま片耳ヘッドホン用DSPの有効性と説明しない。
- 聞こえる側にも聴力の個人差がある。健聴者が片耳を塞いだ試聴だけで対象者への有効性を断定しない。
- 医療的な診断、聴力測定、補聴器・人工内耳の調整を実装しない。効果をうたう文言を変更するときは対応する評価記録を確認する。
- 当面はSPECの段階Aを小さく検証する。段階B以降は先行する知覚評価と技術検証の結果に基づいて扱う。

## 設計と保守

- UI、音声入出力、DSP、評価記録の責務を分ける。単一macOSアプリと小さなDSPライブラリを基本とし、必要のないサービス、プラグイン機構、クラウドを追加しない。
- Swift/SwiftUIとApple標準の音声APIを基本にする。DSPでC/C++が必要なら、リアルタイム制約など理由を示し、境界を狭く保つ。
- DSPは決定的にオフライン検証できる設計にする。パラメータの単位、範囲、初期値、更新時の挙動を明示する。
- リアルタイム音声コールバックで、メモリ確保・解放、ロック待ち、ファイルI/O、ネットワーク、ログ出力、UI操作、Task生成をしない。配列の暗黙コピーやARCによる解放も監査する。
- バッファは事前確保し上限を設ける。制御情報は境界で安全に受け渡す。負荷増大時にキューを無制限に延長しない。
- サンプルレート、チャンネル配置、タイムスタンプ、ストリーム寿命を明示する。変換やリサンプリングを暗黙の挙動に任せない。
- 新しい依存関係・音源・HRTFデータを導入する前に、保守状況と利用・再配布条件を一次資料で確認する。

## 音声とプライバシーの不変条件

- 利用者の操作なしに音を再生しない。初期音量は低くし、OSの音量を上げない。出力先・聞こえる耳を確認して開始する。
- ソフトウェアのdBFS上限は耳元のdB SPL上限ではない。「安全な音圧を保証する」と表示しない。
- すべてのモード、試聴音、切り替えに最終出力ゲインとピーク制限を適用する。比較モードでも制限を迂回しない。
- 選択していない出力チャンネルをゼロにする。出力機器の消失時に内蔵スピーカーへ自動で切り替えて再生しない。
- NaN/Inf、不正なバッファ、音声グラフの異常は無音化・停止で扱う。無音や欠落を音量の自動増加で埋め合わせない。
- システム音声取得では自己プロセスの再取り込み、原音との二重再生、停止後のミュート残留を実機で確認する。API名の存在だけを動作証拠にしない。
- 音声はローカルで処理する。通常利用で音声を保存・送信せず、ログに音声内容やファイルのフルパスを残さない。評価結果の書き出しは明示操作で行う。

## 検証と複数回の自己監査

1. 作業前に「何を事実と考えているか」「仮説は何か」「何が出たら方針を棄却するか」を整理する。過去の採用案を正当化するために根拠を選ばない。
2. 変更後に要求とデータ経路を突き合わせる。左右交換、逆相、無音、過大入力、機器切断、権限拒否など、正常系の外を確認する。
3. 完了報告前に別の観点で再監査する。数値上の成立と知覚上の有効性、静的確認と実行済みテスト、対象者の結果と健聴者の結果を分ける。

- 振る舞いを変える修正では、その問題を再現する意味のあるテストを先に用意する。文書だけの変更に実装をなぞるテストを追加しない。
- 最低限、SPECのDSPテスト、経路・停止テスト、該当段階の実機確認を行う。テスト名、条件、実行結果を記録する。
- ビルド・試聴できない場合は未検証と報告する。過去の合格やソースの読解で、今回の実行結果を代用しない。
- 完了報告は、変更内容、検証結果、残る制約、次の一手を簡潔に示す。効果がなかった結果も残す。

## 文書の扱い

- プロダクト要件・受け入れ条件は `SPEC.md`、作業方針はこのファイルを正とする。変更時は両者の矛盾を解消する。
- 2026-09-09時点では調査と仕様だけが存在する。将来のビルドコマンドや性能目標を「実行済み」と記載しない。
- 依頼されていない `GOAL.md`、追加計画書、実装雛形を自動で作成しない。
