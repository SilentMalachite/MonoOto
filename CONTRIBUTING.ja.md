# MonoOtoへの貢献

**英語版の[CONTRIBUTING.md](CONTRIBUTING.md)が正本です。この文書は日本語訳です。内容に相違がある場合は英語版が優先されます。**

MonoOtoの改善にご協力いただきありがとうございます。本プロジェクトは現在、ステレオ音源の左右チャンネル由来の違いを、片方の聞こえる耳で音色の手がかりによって判別しやすくできるかを探る、小さな段階Aの試作を検証しています。自然なステレオ知覚、聴力の回復、正確な定位、医療上の効果を保証するものではありません。

貢献する前に、[SPEC.md](SPEC.md)と[AGENTS.md](AGENTS.md)を読んでください。`SPEC.md`はプロダクト要件と受け入れ条件の正本です。`AGENTS.md`は、このリポジトリでの作業方法と安全上の制約を定めます。

## 範囲と設計原則

変更は小さく保ち、仕様の現在の段階に結び付けてください。大きな機能、アーキテクチャ変更、新しい依存関係、音源、HRTFデータを導入する前に、GitHub Issueで相談してください。段階Bのシステム音声取得は、`SPEC.md`に定めた段階Aの知覚評価と技術検証によって進行が妥当と判断されるまで開始しません。

次の境界を守ってください。

- UI、音声入出力、DSP、評価記録の責務を分ける。
- Swift、SwiftUI、Appleの音声APIを優先する。CまたはC++は、狭く限定したリアルタイム要件など、必要性を実証できる場合に限る。
- DSPは決定的で、独立してオフラインテストできる状態を保つ。パラメータの単位、範囲、初期値、更新時の挙動を明示する。
- 不要なサービス、クラウド処理、プラグイン機構、アカウント、特権ヘルパー、広範な依存関係を追加しない。
- 音声はローカルで処理する。通常利用時に音声を保存・送信せず、音声内容やファイルのフルパスをログへ記録しない。

科学的根拠、工学上の仮説、実測結果は分けて説明してください。音色の手がかりを判別できたことを、空間的なステレオ知覚の証明として扱わないでください。診断、治療、聴力回復、安全な音圧、効果に関する主張は、その表現と対象者を直接裏付ける根拠なしに行わないでください。

## 開発の流れ

1. 既存Issueを確認し、意図する挙動、範囲、設計について合意が必要ならIssueを作成する。
2. 現在のデフォルトブランチから、対象を絞ったブランチを作成する。
3. 挙動を変える修正や機能では、意味のある回帰テストを先に追加する。
4. 合意した要件を満たす最小の変更を実装する。
5. データ経路全体と異常系をレビューする。該当する場合は、無音、過大入力、NaN/Inf、左右交換、逆相、機器消失、権限拒否、停止、古い非同期処理を含める。
6. 関連する自動検証を実行し、未実施の必要な実機確認を記録する。
7. 問題、変更後の挙動、実施した検証、残る制約を説明するPull Requestを作成する。

無関係な整理を機能変更に混ぜないでください。利用者や並行作業者の変更を保護し、挙動や制約が変わる場合は文書も更新してください。

## ビルドとテスト

パッケージと共有Xcode schemeの標準コマンドは次のとおりです。

```sh
swift test
xcodebuild -project MonoOto.xcodeproj \
  -scheme MonoOto \
  -destination 'platform=macOS,arch=arm64' \
  test
```

開発中は対象を絞ったテストを実行し、レビュー依頼前に関連する全テストを実行してください。単体テストの成功は、機器への配線、聞こえ方、タイミング、知覚上の効果を証明しません。実機確認では、機種、macOSバージョン、サンプルレート、バッファサイズ、試験条件を記録してください。ビルド、試聴、実機テストを実行していない場合は、ソース確認や過去の実行結果から成功と推定せず、未検証と明記してください。

DSPの変更では、通常モノラルとの一致、同一チャンネル入力、チャンネル符号、逆相での相殺、平滑化、有限な出力、最終ピーク上限など、`SPEC.md`の該当する不変条件を決定的に検証してください。合成音、自作音源、または適切なライセンスの素材を使用してください。

## リアルタイム音声の要件

音声レンダーコールバックでは、メモリの確保・解放、ロック待ち、ファイルまたはネットワークI/O、ログ出力、UI更新、Task生成、ARCによる解放を行わないでください。上限のあるバッファを事前確保し、配列の暗黙コピーとオブジェクトの寿命を監査してください。制御情報は、上限がありリアルタイム安全な方法でコールバック境界を越えて受け渡してください。

不正なバッファ、音声グラフの異常、NaN/Infは無音化し、制御側へ通知して停止してください。すべての再生経路と比較経路に、最終ゲインとピークリミッターを適用してください。選択していない出力チャンネルはゼロを保ち、選択した機器が消失した場合は、別の出力先へ暗黙に切り替えず再生を停止してください。

レンダー経路の変更には、ソースレベルのリアルタイム安全性レビューと、対応ハードウェア上での適切なプロファイルが必要です。Releaseビルドの静的確認だけで実行時測定を代替しないでください。

## ローカル解析データ

`graphify-out/`と`.ai-collab/`は、生成グラフ、キャッシュ、環境固有のパス、レビュー実行状態を含むため、コミット対象にしません。現状は未追跡のローカルデータで、リポジトリのignore設定には含まれていないため、意図したファイルだけを明示的にステージしてください。`docs/verification/`の再利用可能なスクリプトは追跡対象の検証ソースです。

## 文書と翻訳

公開プロジェクト文書は英語版を正本とします。日本語訳には対応する`.ja.md`ファイル名を使用します。英語文書を変更するときは、同じPull Requestで日本語訳も更新してください。更新できない場合は、翻訳が一時的に古いことを明記し、追跡可能な後続Issueを作成してください。正本と翻訳の相互リンクを目立つ位置に保ってください。

ライセンスの規範的な文章を、翻訳にも法的効力があるように扱わないでください。英語の`LICENSE`が適用され、各言語の案内は参考情報にすぎないことを明記してください。

## Pull Request

Pull Requestには次の情報を含めてください。

- 問題と変更後の挙動の簡潔な説明。
- 関連する要件またはIssue。
- 実行した自動検証と、その正確な結果。
- 実施した手動確認、実機確認、アクセシビリティ確認、試聴と、その条件。
- 未検証のテストや確認事項と、その理由。
- 利用者、安全性、プライバシー、性能、互換性への影響。

UI変更にはスクリーンショットが役立ちますが、VoiceOverとキーボード操作の確認を代替しません。非公開の音声、個人の聴力情報、ローカルのフルパス、機器UID、認証情報、その他の機微情報を、Issue、テストログ、スクリーンショット、Pull Requestに含めないでください。

## 貢献物のライセンス

本プロジェクトはApache License, Version 2.0でライセンスされます。別途明示しない限り、プロジェクトへの収録を目的として提出した貢献物には、Apache License第5項の定めに従って同じライセンスが適用されます。提出者は、その貢献物を提出する権利を有していなければなりません。第三者のコード、データ、メディア、その他の素材を明示し、必要な通知とライセンス条件をすべて維持してください。ライセンスが不明または本プロジェクトと両立しない素材を提出しないでください。

プロジェクトへの参加には[CODE_OF_CONDUCT.ja.md](CODE_OF_CONDUCT.ja.md)も適用されます。
