# MonoOto — 片耳でステレオ音源を楽しむmacOSアプリ

作成日・調査日: 2026-09-09  
状態: 調査に基づく初期仕様案。DSP・ファイル処理・無音ホストを実装済み。現行macOS／arm64／USB出力でタスク4の無音ホスト検証を完了。音楽再生の統合・聴取評価は未実施。実行結果と残る条件は [段階A検証記録](docs/verification/stage-a.md) を参照。以下の数値は検証用の設計目標であり、効果や対応環境の実績ではない。

## 1. 結論と目的

最初に作るのは、ステレオの左右に異なる音色の手がかりを付け、聞こえる耳へ合成して届ける小さな音楽プレーヤーとする。通常のモノラルより左右由来の違いや主観的な広がりを感じられるかを比較する。効果を確認できた場合に、Core Audio tapsで他アプリの音を処理する段階へ進む。

片耳へのソフトウェア出力だけで両耳間時間差・レベル差の比較機構を回復することは想定しない。目的はユーザーが望む「ステレオ感覚」に近づく体験の探索であり、「本来のステレオ聴覚の再現」を既知の技術として仕様化しない。[単耳聴の一次研究](https://pubmed.ncbi.nlm.nih.gov/25071433/)

以下を別々に測定する。

| 評価対象 | 意味 | 成功だけでは言えないこと |
| --- | --- | --- |
| 左右由来の違い | 音が元のL/Rどちらに含まれていたかを手がかりから判別できる | 頭の外や左右の空間に定位すること |
| 主観的な広がり | 通常モノラルより音に広がり・分離感を感じる | 正しい音源方向や距離の再現 |
| 聞きやすさ・楽しさ | 音色の変化を許容でき、音楽を楽しめる | 医療上の効果や全員への適合 |

## 2. 暫定前提と対象

- 片耳で音を聞く成人が、自分で無理のない音量を調整し、試聴を中断できる利用を初期対象とする。診断名やオージオグラムの入力は必須にしない。
- 初期用途は音楽、初期出力は有線のステレオヘッドホン／イヤホン。聞こえる耳を左・右から選択する。聞こえる側の高域聴力も一律とは仮定しない。
- macOS 14.2以上、Apple Siliconを初期の実機検証対象とする。下限は後続のCore Audio tapsとの整合による設計判断であり、全OS版での動作確認を意味しない。Intelは初期の必須対象外。
- Bluetoothは追加検証対象。Bluetooth固有の遅延・モード変更を含め、ゲーム用の遅延保証をしない。
- ユーザーから用途・機器の指定が得られた場合は、実装前にこの暫定前提と評価を更新する。まだユーザー承認済みの製品受け入れ条件ではない。

## 3. 調査結果と採用判断

### 3.1 科学的根拠の範囲

- Agterbergら（2014）は、片側聾の参加者について、実空間の音源定位に耳介のスペクトル手がかりが寄与することと、大きな個人差を報告した。良聴耳の高域聴力も結果に関係する。これは任意のEQを片耳ヘッドホンへ適用すれば左右のステレオ知覚が成立するという証拠ではない。[論文](https://pubmed.ncbi.nlm.nih.gov/25071433/)
- Firsztら（2015）は、片側の高度〜重度難聴者11人の定位訓練を調べ、改善の可能性と個人差を報告した。スピーカーによる定位訓練の結果であり、本アプリの音楽再生の有効性や必要訓練時間へ直接置き換えられない。[論文](https://pmc.ncbi.nlm.nih.gov/articles/PMC4291285/)
- 今回確認した一次資料には、本仕様の音色符号化で自然なステレオ感が得られることを直接検証したものはない。したがって本方式は工学的な仮説とし、当事者による比較で継続・棄却を判断する。

### 3.2 音響方式の探索記録

以下は調査上の比較であり、並行実装する機能一覧ではない。最初に検証する改善案は音色符号化一つとする。

| 方式 | 得られるもの・検証可能性 | 制約 | 初期判断 |
| --- | --- | --- | --- |
| 通常モノラル合成 | 両チャンネルを片耳へ届ける基準 | 左右の由来を失い、逆相では相殺する | 必須の対照条件 |
| 左右別の音色符号化 | L/Rの違いを単耳で使える手がかりへ変換する仮説 | 音色変化、音源依存、学習、単耳混合の限界 | 最初の試作 |
| 片耳側HRTFの畳み込み・頭部追跡 | スペクトルや動的変化の利用を検討できる | 個人差、頭部姿勢入力、音色の交絡。通常の両耳HRTFを適用するだけでは不足 | 初期案の評価後にのみ再検討 |
| 片側の遅延、残響、ピッチ移動 | 時間・音色差を付けられる | コムフィルタ、音楽の変質、聞き疲れ。単耳のHaas効果を両耳の定位回復と混同できない | 初期対象外 |
| 片耳側に複数ドライバを配置する専用機器 | 耳介へ異なる物理経路で音を届ける研究余地 | 通常イヤホンのDSPだけでは作れない経路。別の機器開発が必要 | ソフトウェア単独の範囲外 |

この表の工学的な採否は本プロジェクトの判断であり、各方式の臨床効果を比較した結論ではない。AI音源分離は最初の方式の成立に不要で、音質・遅延・保守の負担を増やすため導入しない。

### 3.3 macOS音声取得の探索記録

| 方法 | 特徴 | 制約と判断 |
| --- | --- | --- |
| アプリ内の音声ファイル再生 | 左右PCMを確実に用意でき、繰り返し比較しやすい | 他アプリの音は扱えない。段階Aに採用 |
| Core Audio process taps | プロセス音声を取得し、aggregate deviceの入力として使用できる | 権限、原音ミュート、自己除外、機器変更を実機検証。段階Bの第一候補 |
| ScreenCaptureKit | 音声取得と自己プロセス音声の除外を提供する | 音声の取得だけで原音置換は完成しない。権限と原音制御を別に解決する必要があり初期不採用 |
| BlackHole等の仮想音声デバイス | アプリ間で音を渡す既存の経路 | インストール、ルーティング、復旧、配布条件の検討が増える。初期の必須依存にしない |

根拠: [Apple Core Audio tapsサンプル](https://developer.apple.com/documentation/coreaudio/capturing-system-audio-with-core-audio-taps)、[CATapDescription](https://developer.apple.com/documentation/coreaudio/catapdescription)、[ScreenCaptureKitの自己音声除外](https://developer.apple.com/documentation/screencapturekit/scstreamconfiguration/excludescurrentprocessaudio)、[BlackHole公式リポジトリ](https://github.com/ExistentialAudio/BlackHole)。BlackHoleの「追加ドライバ遅延ゼロ」は本アプリ全体の遅延ゼロを意味しない。

## 4. 段階A — 知覚の成立を調べる最小試作

### 4.1 必須のユーザー体験

1. 起動時は停止。目的と「効果は個人差があり検証中」を短く示す。
2. 出力機器と聞こえる耳を選ぶ。明示操作による短い確認音を低い初期音量で再生できる。自動聴力検査はしない。
3. ローカルの非DRM WAV/AIFFを一つ開く。初期対応はモノラル／ステレオ、44.1/48 kHz、PCM 16/24 bitまたはFloat32。非対応形式は理由を表示して停止を保つ。
4. 再生・一時停止・停止・シークを提供する。通常モノラル／音色手がかり付きモノラルを同じ位置で比較できる。
5. 手がかりの強さ、基準周波数、アプリ内音量を調整できる。画面上の左右は入力音源のL/Rであり、出力耳の左右と区別する。
6. ステレオ入力に対してのみL単独／R単独の確認再生を用意する。逆相や片チャンネル欠損を調べる補助であり、空間音響モードとして宣伝しない。
7. 出力機器切断・形式変更・スリープ時に停止する。出力耳・機器の選択変更もいったん停止して反映し、利用者が再開操作するまで鳴らさない。
8. ボタンと調整値に日本語のアクセシビリティラベルを付け、VoiceOverとキーボードで耳選択、比較、停止を操作できる。色だけで状態を伝えない。

入力のモノラルは内部で `L=R=x` と扱い、音色の左右手がかりを生成しない。対応範囲外のマルチチャンネルを暗黙にステレオへ折り畳まない。

### 4.2 DSPの初期実験仕様

以下は検証しやすい出発点であり、聴覚研究から最適値が導かれた処方ではない。

```text
ファイル → デコード・48 kHz Float32へ変換 → L/R
                                              │
                       ┌──────────────────────┼───────────────────┐
                       ↓                      ↓                   ↓
                 通常モノラル M          M + a × Q(S)          L/R単独確認
                       └──────────────────────┼───────────────────┘
                                              ↓
                      モード切替平滑化 → 共通ゲイン → 出力レート変換
                                              ↓
                                       最終ピーク制限
                                              ↓
                          選択耳のチャンネルへ出力・反対側はゼロ
```

- `M[n]=(L[n]+R[n])/2`、`S[n]=(L[n]-R[n])/2`。
- 通常モノラルの制限前信号は `M`。手がかり付きは `M+a×Q(S)`。
- `Q` は一次ローパスとの差によるハイパスとする。`c=exp(-2πfc/fs)`、`p[n]=(1-c)S[n]+c p[n-1]`、`Q(S)[n]=S[n]-p[n]`。`fs=48000`、初期 `fc=1500 Hz`、調整範囲 `800〜4000 Hz`。
- 強さ `a` は `0〜0.6`、初期値 `0.35`。音色モード自体は利用者が選ぶまで有効にしない。通常モノラルを初期モードとする。
- これは `0.5×[(I+aQ)L+(I-aQ)R]` と等価。L/Rに異なる高域の形を付け、`L=R` の中央成分を変えにくくする意図である。位相も変化するため、実際の周波数応答を検証する。
- 出力耳を切り替えても、入力L/Rに付ける符号は変えない。左耳なら `(y,0)`、右耳なら `(0,y)`。出力のチャンネル配置が判別できない機器は開始しない。
- シーク・停止・入力／出力機器交換時に、フィルタと制限器の状態を初期化し、デコード先読み、制限器の先読み、アプリ内の出力待ち音声を破棄する。強さ、周波数、モードの変更は20 ms以上で平滑化し、開始は100 msでフェードインする。異常時は滑らかさより即時無音化を優先する。
- 44.1 kHz入力は制御／デコード側で48 kHzに変換する。出力デバイス側に別のレートが必要なら出力境界で変換し、DSP本体の式を変更しない。

### 4.3 合成の限界と逆相

- 任意の2チャンネルを1チャンネルへ合成して、情報を可逆に保持することは本方式ではできない。「左右の全情報を必ず失わず再現」と約束しない。
- `L=-R` では通常モノラルは無音になる。提案方式でも消える周波数や組み合わせがある。差成分の追加は万能な相殺対策ではない。
- `L=-R` の左右交換は出力の符号反転にしかならないことがあり、安定した左右判別の手がかりとはしない。純音では音色の差がレベル差に縮退し得る。
- 多数の楽器・残響を含むミックスでは、元の音色と符号化した音色を分離できない場合がある。ステレオ音源のL/Rは物理空間の方位そのものではない。
- 入力に十分なエネルギーがあり通常モノラル合成で大幅に減る場合は「位相の影響で音が小さくなる可能性」を表示する。検出条件は100 ms窓の `E(M)/((E(L)+E(R))/2+1e-12)<0.1` が500 ms以上継続、入力RMSが−60 dBFS超。`E` は窓内平均二乗、入力RMSは `sqrt((E(L)+E(R))/2)` とする。これは診断用の目安で、可聴性の判定ではない。
- 警告時も勝手に片チャンネルへ切り替えず、音量を上げない。利用者はL/R単独確認で音源を調べられる。

### 4.4 出力レベルと停止

- アプリ内の初期ゲインは−18 dB、調整範囲はミュート〜0 dB。OSや外部機器の音量は変更しない。
- アプリが行うすべてのレート変換・ゲイン処理の後に、出力レートで5 msの先読みピークリミッターを置き、リリースの初期値は100 ms。アプリから渡す出力サンプルの絶対値を `10^(-3/20)` 以下とする。比較、L/R単独、確認音の全経路に適用する。OS・機器内部の後段処理はこの上限の保証範囲に含めない。
- dBFSはデジタル値であり、イヤホン感度、DAC、OS音量による実際の音圧は保証できない。上記は聴覚保護を保証する閾値ではない。真のピーク／アナログ段のクリッピングも別に評価する。
- 音が小さい箇所や無音を自動で持ち上げるAGCは実装しない。リミッターの作動が多い比較音源は、比較前に共通ゲインを下げる。
- ソフトウェア異常、NaN/Inf、バッファ不整合は無音出力し、制御側へ通知して停止する。停止操作で新しい音声供給は100 ms以内に止めることを目標とし、デバイスに既に渡した音声の遅延とは区別する。
- 大きさを無理に上げず、違和感・痛み・耳鳴り・疲労が出た場合は中断できるUIにする。自動で高音量まで上げる校正は行わない。

## 5. 技術構成

- 単一のSwiftUI macOSアプリ。UI／設定と状態遷移はSwift、音声経路はAVAudioEngine、AVAudioPlayerNodeまたはAVAudioSourceNode等の標準APIを基本とする。APIを使うだけでリアルタイム安全になるとは考えない。[AVAudioSourceNode](https://developer.apple.com/documentation/avfaudio/avaudiosourcenode)
- DSPは副作用の小さい独立ライブラリとし、オフラインと再生時で同じ処理を使う。最初は固定係数フィルタと制限器に限定する。
- ファイルのデコード・先読みとUIはリアルタイムコールバックから分離する。事前確保した有界バッファを使用し、枯渇時はゼロを供給して通知する。上限超過時は無制限に遅延をためず停止する。
- 状態は `stopped → preparing → running/paused → stopping → stopped` とし、異常・機器消失・権限失効は `error` を経て停止する。準備完了前に音声を出さない。終了処理は重複して呼ばれても安全にする。停止・シーク・入力／出力交換で再生世代を更新し、旧世代の準備・デコード・コールバック完了は音声や状態を復活させない。
- 設定はローカルに、出力耳・機器UID・音量・DSP値・設定バージョンだけを保存する。再起動時は停止状態。保存値の範囲を検証する。音声、診断、聴力情報を収集しない。
- 想定する実装単位は `App/UI`、`Audio/Input・Output・Lifecycle`、`DSP`、`Evaluation`、`Tests`。これは責務の区分であり、今の段階でディレクトリや抽象化を一括生成する要求ではない。

## 6. 段階B — 他アプリの音声処理

段階Aの評価に基づいて進行を判断する後続範囲。現時点では実装を開始しない。

### 6.1 入力と権限

- まず一つの選択アプリのステレオ音声を取得する。アプリが複数プロセスから音を出す場合は音声プロセスの対応付けを確認し、不完全な取得を「対応済み」としない。
- Apple公式サンプルではmacOS 14.2以上と `NSAudioCaptureUsageDescription` が必要で、tapを含むaggregate deviceからの初回取得時にシステム音声の許可が要求される。対象SDKの可用性と実OSの権限表示を実装時に再確認する。[Appleサンプル](https://developer.apple.com/documentation/coreaudio/capturing-system-audio-with-core-audio-taps)
- API可用性、署名、配布形態、App Sandbox下の可否は実機スパイクで確認する。未知のentitlementや非公開APIを推測で足さない。権限拒否時には理由と再設定方法を示し、元の音声経路を変えない。
- DRM等により取得できない音声は対応外とし、保護を回避しない。無音バッファだけからDRMと断定せず、無音・権限・非対応を切り分ける。

### 6.2 経路と復旧の契約

```text
選択アプリ → process tap → private aggregate device
                                      ↓
                       有界バッファ・クロック調整
                                      ↓
                           段階Aと同じDSP・制限器
                                      ↓
                              選択した出力機器
```

- `CATapDescription` によるステレオ入力と自己音声除外を検証する。自アプリと自前音声ヘルパーは取得対象にしない。全体取得は単一アプリで成立した後の範囲とする。[CATapDescription](https://developer.apple.com/documentation/coreaudio/catapdescription)
- 原音を通常出力したまま処理音を重ねない。出力が準備できてからtap取得を開始し、取得中に原音をミュートする構成を検証する。`mutedWhenTapped` を候補とするが、停止・異常終了時の復帰はAPI名から保証しない。[CATapMuteBehavior](https://developer.apple.com/documentation/coreaudio/catapmutebehavior)
- 通常モノラルへの比較切替はDSP内で行う。tapを解除する停止操作と、DSPの比較モードを区別する。
- 通常停止は処理音を止め、取得を止め、tap・aggregate device・リスナーを解放する。途中失敗時も作成済み資源を後始末する。
- tap解放後は対象アプリの原音が元の経路で再開し得る。この原音にはMonoOtoの音量制限が効かない。開始前に原音側の音量も低くしてもらい、原音復帰の挙動を説明する。
- 切断・クラッシュ時に、MonoOto自身はスピーカーへ切り替えて再生しない。ただしmacOS／元アプリによる原音の出力先変更は完全には管理できない。全システムの無音を保証しない。
- 入出力クロック差、デバイスのレート変更、長時間運転に対するドリフト補正と有界バッファを検証する。対応未確認の経路を音切れしないと表現しない。
- 他アプリへの影響を含む機器切断、スリープ、権限取消、強制終了を評価用環境で試す。原音復帰や自己除外が成立しない場合、段階Bの配布を進めない。

### 6.3 性能の初期目標

- 有線出力、48 kHz、256 framesを基準条件とする。256 framesは約5.33 msだが、これはエンドツーエンド遅延ではない。
- DSPと先読み制限器のアルゴリズム遅延は10 ms以内を目標とする。段階Bのcapture-to-output遅延は有線経路でp95が30 ms以下を目標とし、取得・バッファ・レート変換・デバイスを含む実測方法と機器を記録する。
- コールバック処理時間p99が、そのバッファの時間枠の25%以下を目標とする。Apple Siliconの検証機種、OS、バッファサイズを併記する。
- 60分再生で音切れ、バッファ上限超過、NaN/Inf、継続的なメモリ増加がないことを確認する。Bluetoothは別測定として報告する。
- これらは製品判断の暫定目標である。未達を隠して全体平均だけを示さず、最悪値、外れ値、発生条件も残す。

## 7. 検証と受け入れ条件案

### 7.1 工学的な受け入れ条件

| ID | 条件 | 検証方法 |
| --- | --- | --- |
| T1 | `a=0` が通常モノラルに一致する | 制限前PCMの最大絶対誤差 `≤1e-6`、異なるブロック分割でも確認 |
| T2 | `L=R` では音色モードの影響がない | 状態初期化後に無音・波形・音楽で制限前誤差 `≤1e-6`。モノラル入力の展開も確認 |
| T3 | L/Rの符号化と出力耳が独立する | Lのみ／Rのみのインパルス・広帯域信号で式との一致、左右耳切替で同一波形が反対チャンネルへ移ることを確認 |
| T4 | 相殺を隠さない | 逆相、位相掃引、強く相関する音源でモノラルの相殺と警告閾値・継続時間を確認。手がかり方式でも相殺が残る例を保存 |
| T5 | すべての経路に制限が効く | フルスケール、±4の過大入力、無音、NaN/Infで有限値・上限・無音化を検証。確認音と単独確認にも適用。全対応出力レートで変換後のアプリ最終出力バッファを確認 |
| T6 | 設定変更と寿命が破綻しない | 再生中の強さ／周波数変更、シーク、耳変更、連続開始停止、ファイル末尾、デコード失敗を確認。準備中の停止、停止直後の再開始で旧世代の音声・処理が復活しない |
| T7 | 片耳以外に意図せず出力しない | ルーティング直後の反対チャンネルが厳密にゼロ。実機ではOSのモノラル化・バランス・機器DSPも確認 |
| T8 | リアルタイム経路が有界である | ソース監査とプロファイルで確保・ロック待ち・ログ・期限超過を確認。バッファ枯渇／上限超過を意図的に発生させる |
| T9 | 機器切断と再開が仕様どおり | 有線切断、デバイス削除、レート変更、スリープから戻した後も停止を保つ実機テスト。機器交換時に待ちバッファの残音や旧世代の完了が再生されない |
| T10 | 段階Bの原音制御が成立する | 権限拒否・取消、開始失敗、通常終了、強制終了、対象アプリ再起動で二重音、ループ、ミュート残留を確認 |

T1〜T9が段階Aの対象、T10と段階6.3のcapture-to-output測定は段階Bで必須とする。試験に使う素材は自作・合成または利用許諾を確認したものとする。

DSP・音声境界のSwift Packageと、アプリの共有scheme `MonoOto` を用意済みであり、検証コマンドは次を標準とする。実行結果は段階A検証記録に条件とともに記載する。

```sh
swift test
xcodebuild -project MonoOto.xcodeproj -scheme MonoOto -destination 'platform=macOS' test
```

プロジェクト構成を変える場合は、実際に動くコマンドへ本節を更新する。CIのDSPテストだけで、音声権限・配線・実際の聞こえの試験を代替しない。

### 7.2 当事者による知覚評価

まず本人が任意に試せる探索評価とする。大規模な有効性の主張は行わない。数値基準は開発を進めるための候補であり、臨床的な有効性の閾値ではない。

- 工学的な出力確認を先に済ませる。聞こえる耳だけへ送り、macOSのモノラル化、空間オーディオ、外部EQ、機器の補正設定を確認して記録する。勝手にOS設定を変更しない。
- 短い練習でL/Rの手がかりを提示する。最初は最大5分を目安とし、いつでも休止・中止できる。効果がないときに繰り返しを強要しない。
- 比較条件は通常モノラルと手がかり付き。同じ原音、同じ耳、同じ機器で比較する。評価者に処理名を見せず順序をランダム化する。ただし音色で条件を推測できるため完全な盲検とは記述しない。
- 評価用の音は事前にオフラインでレベルを合わせる。まず非無音区間のRMSを概ね±0.5 dBにそろえ、音色差による主観音量差も低音量で確認する。調整・練習・本試験の素材を分け、DSP値と校正値は本試験中固定する。正答ラベルや再生順から条件が漏れないようにする。
- **左右由来の判別:** 同一の原音をLまたはRに割り当てた2択課題。条件ごとに60試行、L/R各30を無作為順にし、休憩できる短いブロックへ分ける。練習と本試験で原音を分け、未知の楽器・音声・広帯域音を含める。正解のフィードバックは本試験では出さない。
- レベルだけで正解しないよう左右をレベル整合し、その後にラベルと独立した小さいレベル変動（初期案±3 dB）を加える。余裕のある共通ゲインでリミッターが作動しない条件を使う。純音や狭帯域の失敗も記録する。
- 判別の継続目安は、手がかり条件が通常モノラルより15ポイント以上高く、正答率のWilson 95%区間下限が50%を超えること。別日に同じ手順を未知音源で再評価する。これは課題内での音色手がかりの利用であり、自然な方向定位の証明ではない。
- **音楽体験:** 学習用と別の5曲・各20〜30秒程度を、順序を変えて比較する。「広がり」「音の分離」「自然さ」「楽しさ」「疲れ」を各7段階で別々に回答する。実際のミックスで弁別課題の効果が消えるかも確認する。
- 音楽用途の継続目安は、広がりが1点以上改善する曲が5曲中3曲以上あり、その傾向を別日にも確認できること。自然さ・楽しさの中央値が対照より1点超悪化しないこと、疲れが増えず中止を要する不快感がないことも確認する。
- 少人数の結果は個人・セッション別に報告する。反復試行を参加人数として数えず、Wilson区間はその人の課題成績の記述として扱い、集団への効果を推定しない。中断、無効試行の理由、全試行数、低成績・悪化例も残す。片耳を塞いだ健聴者の結果は補助情報として分ける。
- 評価結果は任意のローカルJSON書き出しとし、匿名ID、条件、DSP値、試行数・正誤、主観評点、機器・OS、評価日時を記録する。音声やフルパス、診断情報を含めない。本人が書き出すまで永続保存しない。

### 7.3 継続・棄却の判断

- 音楽体験の改善と許容可能な音質が確認できた場合に、段階Bへ進むことを検討する。少数の評価は適合した利用者に関する探索結果として扱う。
- 左右判別だけが改善し、広がり・楽しさが改善しなければ、当初目的の達成とはしない。「手がかり弁別の試作」として結果を残し、用途変更を利用者と相談する。
- 広がりだけが改善し左右判別が改善しない場合は、「方向が分かる」と表示しない。実際に改善した体験だけを説明する。
- 差がない、音色劣化が大きい、疲労が増す場合はこの方式を棄却または再設計する。UIや全体音声取得の実装量を増やして、知覚上の不成立を先送りしない。

## 8. 初期範囲外

- 自然な両耳ステレオ、正確な方位・距離、音声認識改善、聴力回復の保証。
- 補聴器・人工内耳の制御、医学的診断、自動聴力測定、外界の警告音検出、マイクを使う常時集音。
- 初期の全システム音声処理、ゲーム同期、動画補正、頭部追跡、AI音源分離、HRTFの個人測定、AU/VSTプラグイン。
- 専用イヤホン開発、仮想ドライバの同梱、管理者権限を要求する常駐ヘルパー、クラウド処理、アカウント。
- App Store対応、署名・公証・配布方式の確定。段階B以降の配布を決めるときに、API実機検証と依存の条件を踏まえて判断する。

## 9. 実施順序と現在の完了範囲

1. **実装済み:** 調査・仕様、音色符号化、共通出力制限、ファイル処理、機器固定と停止を確認する無音ホスト。自動テストとUSB・48 kHzの開始停止を確認済み。
2. **次の一手:** タスク5の有界キューを実装する。タスク4は現行環境で機器変更・スリープ等の停止と44.1／48 kHzのrender・破棄順序を確認して完了した。CoreAudioの停止末尾には同期IPC・待ちが観測されており、音声スレッド全体の無待機は保証しない。条件と制約は[段階A検証記録](docs/verification/stage-a.md)を参照。タスク5・6は未着手。
3. 工学的な確認後、対象者が低音量で試聴し、7.2の比較を行う。結果と未達を記録する。
4. 体験改善が確認できた場合のみ、Core Audio tapsの取得・原音制御・復旧スパイクへ進み、段階Bの可否を判断する。

無音ホストの開始停止確認は、段階A全体の実装・性能測定・当事者評価・OS全体の音声処理の完了を意味しない。

## 10. 主要な一次資料

すべて2026-09-09にオンラインで確認。仕様中のDSP式、数値初期値、進行基準はプロジェクトの仮説・判断であり、これらの資料が直接推奨する値ではない。

1. [Agterberg et al., 2014 — Single-sided deafness and directional hearing](https://pubmed.ncbi.nlm.nih.gov/25071433/) — 単耳のスペクトル手がかりと個人差。
2. [Firszt et al., 2015 — Localization Training Results in Individuals with Unilateral Severe to Profound Hearing Loss](https://pmc.ncbi.nlm.nih.gov/articles/PMC4291285/) — 実空間での定位訓練の探索研究。
3. [Apple — Capturing system audio with Core Audio taps](https://developer.apple.com/documentation/coreaudio/capturing-system-audio-with-core-audio-taps) — tap、aggregate device、権限、サンプルのOS下限。
4. [Apple — CATapDescription](https://developer.apple.com/documentation/coreaudio/catapdescription) / [CATapMuteBehavior](https://developer.apple.com/documentation/coreaudio/catapmutebehavior) — 取得対象、ミックス、ミュートのAPI。
5. [Apple — AVAudioSourceNode](https://developer.apple.com/documentation/avfaudio/avaudiosourcenode) — レンダリング用の音声供給API。
6. [Apple — SCStreamConfiguration.excludesCurrentProcessAudio](https://developer.apple.com/documentation/screencapturekit/scstreamconfiguration/excludescurrentprocessaudio) — ScreenCaptureKitの自己音声除外。
7. [Existential Audio — BlackHole](https://github.com/ExistentialAudio/BlackHole) / [LICENSE](https://github.com/ExistentialAudio/BlackHole/blob/master/LICENSE) — 仮想音声経路と導入・配布検討の出発点。今回は導入していない。
